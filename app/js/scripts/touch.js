/*
 * TOUCH
 */

(function($) {

    $.touch = function() {

        var isTouch = 'ontouchstart' in document.documentElement;

        // TOUCH DEVICE SCRIPTS
        if(isTouch) {
            // TOUCH FRIENDLY DROP DOWN MENU
        }

        // NO TOUCH DEVICE SCRIPTS
        if(!isTouch) {
            // ADD SCRIPTS
        }

    };

}(jQuery));
